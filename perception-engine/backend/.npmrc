# Inherits intent from the repo-root .npmrc; duplicated here so Docker
# builds (which COPY only the workspace dir) still enforce the engines
# constraint and refuse Node versions below the 25.5.0 baseline.
engine-strict=true
